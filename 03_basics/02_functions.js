//rest(...para) operator - to take n arguments in single para(store all the arg in a singl array)

function restworking(var1,var2,...num)//...para - rest operator // same syntax as spread
{
    return num
}

let res = restworking(200,344,12,90,4532)
// console.log(res)

